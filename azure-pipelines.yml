# Maven
# Build your Java project and run tests with Apache Maven.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/java

trigger:
- main

variables:
  imageRepo: demo
  tag: '$(Build.BuildId)'
  myApp: spring-azure-demo

stages:
- stage: Build 
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: Maven@3
      inputs:
        mavenPomFile: 'pom.xml'
        mavenOptions: '-Xmx3072m'
        javaHomeOption: 'JDKVersion'
        jdkVersionOption: '1.8'
        jdkArchitectureOption: 'x64'
        publishJUnitResults: true
        testResultsFiles: '**/surefire-reports/TEST-*.xml'
        goals: 'package'
    - task: BuildpacksInstaller@1
      inputs:
        buildpacksVersion: '0.16.0'
    - task: BuildpacksTask@1
      inputs:
        command: 'build'
        imageName: '$(imageRepo)/${myApp}:${tag}'
        builder: 'paketobuildpacks/builder:base'
        buildpacks: 'gcr.io/paketo-buildpacks/java-azure'
        envVariables: 'MAVEN_OPTS=-Xms256m -Xmx512m'
        projectPath: 'target/spring-azure-demo-0.0.1-SNAPSHOT.jar'
        pullPolicy: 'always'
        
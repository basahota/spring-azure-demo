apiVersion : apps/v1
kind: Deployment
metadata:
  name: springazuredemo 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: springazuredemo
  template:
    metadata:
      labels:
        app: springazuredemo 
    spec:
      containers:
        - name: springazuredemo 
          image: #k8simage#
          ports:
          - containerPort: 80
